---
tags:
  - リファクタ
  - todo
---

# ディレクトリ構造の大幅な変更におけるTODO

## 主なtodo

### src/shared/components

1. 主要な変更点
   - 共通するコンポーネントの移植、まとめ
   - index.tsxの設置
2. todo
   - index.tsxの役割: すべてのタブに共通して描けるようなエクスポート ✓
   - この役割に乗っ取りプログラムを記述　✓
   - 必要であれば、表示の有無など状態管理のためのロジックを設計 => 配置場所注意　✓
   - 他に共通ので利用するhooksが必要になった場合は shared/hooksに配置
   - SelfBottomTabsに゙関して => 責務分離を行う

### src/features

1. 主要な変更点
   - 各タブごとで異なるuiやインフラなどをフォルダごとに分離
   - 現状、homeを除いた他のフォルダは空
   - homeにmap, searchタブを作成 => それぞれで実装
   - mapには最適化されたディレクトリ設計を実装 => 詳しくは、srcRuleを参照
2. todo
   - 現状実装されているsourceフォルダの責務分離
   - MapScreen.tsxやcache系の責務が思いファイルのロジック分離
   - 各フォルダに実装した部品の集約などの役割を持つファイルの実装

### src/infra

1. 主要な変更点
   - fileCache.tsの作成
2. todo
   - infraファイルの目的は、特定のタブではなく全体で利用できるものにすること
   - fileCache => cache読み込みのロジックを整理したうえで、FileSystemをmapだけでなく全体で共通して扱えるよう実装

## その他 問題修正タスク

- map表示においてタブ切替時に毎回再描画されてしまう
- mapマウント時に、cameraが遅れて正しい位置にカメラが来ない

## 学習系

- 実装したbottomTabBarの挙動理解
